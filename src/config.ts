// Config for spacing and timeline
export const config = {
    timelineHeight: 500,   // total height in px for the timeline
    cardWidth: 208,        // initial horizontal offset (w-52)
    spacingX: 250,         // horizontal spacing between nodes
    pageMargin: 20,        // page left margin
};
